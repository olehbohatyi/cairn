# cairn

This is the mdoc-processed docs site placeholder. Nothing here is a working
example yet — the target API is sketched in the repository's root README
(`README.md`) but not implemented (see the roadmap in `CLAUDE.md`). Real
`scala mdoc` snippets land here as each piece ships; every snippet added must
compile.
